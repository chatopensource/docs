---
title: "Development"
description: "Install the Hackerforms CLI, AWS CLI, and Asimov CLI to preview changes locally"

---

<Tabs>
  <Tab title="pip">
  Using the [pip](https://pypi.org/project/pip/) package manager
  
  Visit <a href='https://app.getasimov.com/key'>Asimov AI</a> and the free commercial license key will be sent to your email

  ```bash
  pip install hackerforms
  pip install awscli
  ```
  </Tab>
  <Tab title="brew">
  Using the [homebrew](https://brew.sh/) package manager
    
  Visit <a href='https://app.getasimov.com/key'>Asimov AI</a> and the free commercial license key will be sent to your email
  
  ```bash
  brew install python@3.10
  brew install awscli
  ```
  </Tab>
</Tabs>

## 🌥️ Create an AWS account, if one does not already exist.
You will need to setup your root account, login to the [AWS account console](https://console.aws.amazon.com/console/home)
## 🔒 Create a new IAM user in the AWS account called "asimov", and grant it the following settings.
Setup a new user called asimov if you don't already have a role [using IAM](https://console.aws.amazon.com/iamv2/home#/roles/create?step=selectEntities))

The next step is to setup permissions and you should select "Attach existing policies directly" and then choose:

[AWSBillingReadOnlyAccess](https://console.aws.amazon.com/iam/home#/policies/arn%3Aaws%3Aiam%3A%3Aaws%3Apolicy%2FAWSBillingReadOnlyAccess)

[CloudWatchReadOnlyAccess](https://console.aws.amazon.com/iam/home#/policies/arn%3Aaws%3Aiam%3A%3Aaws%3Apolicy%2FCloudWatchReadOnlyAccess)

[GetCostForecast](https://console.aws.amazon.com/iam/home#/policies/arn%3Aaws%3Aiam%3A%3A923082272114%3Apolicy%2FGetCostForecast)

[PricingGetProducts](https://console.aws.amazon.com/iam/home#/policies/arn%3Aaws%3Aiam%3A%3A923082272114%3Apolicy%2FPricingGetProducts)

[AmazonEC2ReadOnlyAccess](https://console.aws.amazon.com/iam/home#/policies/arn%3Aaws%3Aiam%3A%3Aaws%3Apolicy%2FAmazonEC2ReadOnlyAccess)

[AmazonRDSReadOnlyAccess](https://console.aws.amazon.com/iam/home#/policies/arn%3Aaws%3Aiam%3A%3Aaws%3Apolicy%2FAmazonRDSReadOnlyAccess)

Create a policy called AmazonWorkspacesReadOnlyAccess with the following permissions

```
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Sid": "VisualEditor0",
            "Effect": "Allow",
            "Action": [
                "workspaces:DescribeConnectionAliasPermissions",
                "workspaces:DescribeAccount",
                "workspaces:DescribeWorkspaceImages",
                "workspaces:ListAvailableManagementCidrRanges",
                "workspaces:DescribeWorkspaceSnapshots",
                "workspaces:DescribeWorkspaceBundles",
                "workspaces:DescribeClientProperties",
                "workspaces:DescribeAccountModifications",
                "workspaces:DescribeConnectClientAddIns",
                "workspaces:DescribeWorkspacesConnectionStatus",
                "workspaces:DescribeConnectionAliases",
                "workspaces:CreateTags",
                "workspaces:DeleteTags",
                "workspaces:DescribeTags",
                "workspaces:DescribeWorkspaceImagePermissions",
                "workspaces:DescribeIpGroups",
                "workspaces:DescribeWorkspaceDirectories",
                "workspaces:DescribeWorkspaces",
                "workspaces:DescribeClientBranding"
            ],
            "Resource": "*"
        }
    ]
}
```

Create a policy called AmazonECRReadOnlyAccess with the following permissions
```
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Sid": "VisualEditor0",
            "Effect": "Allow",
            "Action": [
                "ecr:GetRegistryPolicy",
                "ecr:DescribeImageScanFindings",
                "ecr:GetLifecyclePolicyPreview",
                "ecr:GetDownloadUrlForLayer",
                "ecr:DescribeRegistry",
                "ecr:DescribePullThroughCacheRules",
                "ecr:DescribeImageReplicationStatus",
                "ecr:GetAuthorizationToken",
                "ecr:ListTagsForResource",
                "ecr:ListImages",
                "ecr:BatchGetRepositoryScanningConfiguration",
                "ecr:GetRegistryScanningConfiguration",
                "ecr:UntagResource",
                "ecr:BatchGetImage",
                "ecr:DescribeImages",
                "ecr:TagResource",
                "ecr:DescribeRepositories",
                "ecr:BatchCheckLayerAvailability",
                "ecr:GetRepositoryPolicy",
                "ecr:GetLifecyclePolicy"
            ],
            "Resource": "*"
        }
    ]
}
```
Attach both policies directly to the role

On the review page click "Create Role"

## 🔑 use your account ID and role to onboard
Use this role in the [cloud version](https://app.getasimov.com) of Asimov AI

## Previewing Changes Locally

Run the following command at the root of your repo. Abstra will open in your local browser automatically.

```bash Terminal
python3 AWSGrader.py
```

## Troubleshooting

Here's how to solve some common problems when working with the CLI.

<AccordionGroup>
  <Accordion title="stripe.error.AuthenticationError: No API key provided">
    Input a your own Stripe API key.
  </Accordion>
</AccordionGroup>
